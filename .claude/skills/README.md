# Repo skills

Per root `CLAUDE.md` rule 6: when you learn something non-obvious about this repo, its
tooling, or its APIs, capture it here as `.claude/skills/<kebab-case-name>/SKILL.md` with
frontmatter:

```markdown
---
name: <kebab-case-name>
description: One line saying when this skill applies.
---

The learning, with enough context to act on it.
```
